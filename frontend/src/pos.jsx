import React from 'react'
import ReactDOM from 'react-dom/client'
import POSApp from './components/POSApp'

ReactDOM.createRoot(document.getElementById('react-pos')).render(
  <React.StrictMode>
    <POSApp />
  </React.StrictMode>
)
